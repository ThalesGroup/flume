- [x] Bug in v2/flumetest: nested Start() calls end up discarding all the logs after the nested Start() is reverted.  The parent's buffer is never restored.  Maybe what should happen: either ignored the nested Start(), or when nested Start is reverted, if test hasn't failed yet, drop the nested buffer.  If it has failed, append the nested buffer to the parent buffer, so those logs appear inline in the parent's buffer.
- [ ] Add an option to Config for v1 compatibility
  - installs the DetailedErrors ReplaceAttr
  - And what else?
- [ ] Docs
- [ ] flumetest, and could this be replaced by https://github.com/neilotoole/slogt/blob/master/slogt.go
- [ ] LoggerWriter, could this be replaced by an off the shelf sink?
- [ ] should the attrs passed to Log() also be checked for LoggerKey?
